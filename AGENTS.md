# Rules for AI agents

This repository is public. Do not commit real hosts, IP addresses, tokens,
passwords, SSH keys, `.env` files, or user-specific local paths.

Use placeholders such as `your-host.example.com` and `<server-ip>` in docs and
examples.
